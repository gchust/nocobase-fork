import { Plugin } from '@nocobase/server';

export class BlockCloudServerPlugin extends Plugin {
  async load() {
    // Server-side logic can be added here in the future if needed.
  }
}

export default BlockCloudServerPlugin;
